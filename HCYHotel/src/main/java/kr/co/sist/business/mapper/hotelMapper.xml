<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.business.hotelMapper">

<select id="searchHotel" resultType="String" parameterType="String">
select hotelname
from   hotel
where  id=#{id}
</select>
 <select id="searchReservation" resultType="bhd" parameterType="String">
    select roomcode, id, bookingname, status, pplCnt, checkin, checkout
    from roombooking
    where checkin &lt;= #{selectdate}
    and checkout &gt;= #{selectdate}
</select>

</mapper>