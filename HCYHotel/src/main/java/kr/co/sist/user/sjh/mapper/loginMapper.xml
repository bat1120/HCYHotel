<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.sjh.loginMapper">

<select id="selectLogin" resultType="String" parameterType="kr.co.sist.user.login.LoginVO">
select id
from member
where id=#{id}and password=#{password}
</select>

<!-- <select id="selectMemId" resultType="String" parameterType="kr.co.sist.user.login.findid.FindIdVO">
sele.builder.ct id
from member
where name=#{name} and tel=#{tel} and identifier=#{identifier}
</select>

<select id="selectMemPassword" resultType="String" parameterType="kr.co.sist.user.login.findpassword.FindPassVO">
select password
from member
where id=#{id} and name=#{name} and identifier=#{identifier}
</select>

<update id="updateMemPassword" parameterType="kr.co.sist.user.login.findpassword.FindPassVO">
update member
set password=#{password}
where password=#{password}
</update>

<select id="selectLogin" resultType="String" parameterType="kr.co.sist.user.login.LoginVO">
select id
from business
where id=#{id}and password=#{password}
</select>

<select id="selectBusinessId" resultType="String" parameterType="kr.co.sist.user.login.finiId.FindIdVO">
select id
from business
where name=#{name} and tel=#{tel} and identifier=#{identifier}
</select>

<select id="selectBusinessPassword" resultType="String" parameterType="kr.co.sist.user.login.findpassword.FindPassVO">
select password
from business
where id=#{id} and name=#{name} and identifier=#{identifier}
</select>

<update id="updateBusinessPassword" parameterType="kr.co.sist.user.login.findpassword.FindPassVO">
update business
set password=#{password}
where password=#{password}
</update> -->




</mapper>