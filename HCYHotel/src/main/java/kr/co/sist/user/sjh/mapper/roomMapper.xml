<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.sjh.roomMapper">


<!-- <select id="selectRoomType" parameterType="String" resultType="kr.co.sist.user.hotelsearch.RoomTypeDomain">
select r.*, h.*, i.filename, rt.*
from room r
left outer join hotel h on r.hotelcode = h.hotelcode
left outer join infofile i on r.roomcode = i.roomcode
left outer join roomtype rt on r.hotelcode = rt.hotelcode
where r.hotelcode = #{hotelcode}
</select> -->

</mapper>